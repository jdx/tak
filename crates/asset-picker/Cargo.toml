[package]
name = "asset-picker"
version = "0.0.2"
edition = "2024"
description = "Pick the right release asset for a target platform. Extracted from mise."
keywords = ["release", "assets", "platform", "github"]
categories = ["development-tools"]
readme = "README.md"
license = "MIT"
repository = "https://github.com/jdx/tak"

[dependencies]
regex = "1"
